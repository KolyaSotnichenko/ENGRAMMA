{
  "dependencies": {
    "@nestjs/mapped-types": "*",
    "framer-motion": "^12.23.25",
    "lucide-react": "^0.555.0"
  },
  "name": "engramma",
  "private": true,
  "scripts": {
    "install": "cd backend-nest && npm install",
    "build": "cd backend-nest && npm run build",
    "start": "cd backend-nest && npm start"
  },
  "devDependencies": {
    "@types/express": "^5.0.5"
  }
}
